1. Write a docker-compose.yaml + .env
2. pip install psycopg2-binary : A PostgreSQL database adapter for Python to interact with a PostgreSQL database.
3. pip install python-dotenv: A Python package that is used to manage & store environment variables, it reads key-value pairs from the ".env" file.